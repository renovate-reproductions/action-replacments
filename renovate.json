{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "description": "Use `semver-partial` for action updates",
      "matchDepTypes": ["action"],
      "versioning": "semver-partial"
    }
  ]
}
